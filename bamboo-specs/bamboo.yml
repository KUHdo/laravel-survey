project:
  key: LVLSUR
  plan:
    key: TAP
    name: Test
stages:
  - jobs:
      - scripts:
          - composer install
          - phpunit --log-junit test-reports/phpunit.xml
        requirements:
          - Composer
          - PHPunit composer
        testParsers:
          - junit
